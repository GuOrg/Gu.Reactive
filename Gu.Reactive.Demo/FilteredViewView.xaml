<UserControl x:Class="Gu.Reactive.Demo.FilteredViewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:demo="clr-namespace:Gu.Reactive.Demo"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DataContext="{d:DesignInstance demo:FilteredViewViewModel}"
             d:DesignHeight="300"
             d:DesignWidth="300"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <GroupBox Grid.Row="0" Header="Vanilla observable collection">
            <DataGrid ItemsSource="{Binding PeopleRaw}" />
        </GroupBox>

        <GroupBox Grid.Row="1" Header="Readonly observable collection">
            <DataGrid ItemsSource="{Binding PeopleReadonly}" />
        </GroupBox>

        <GroupBox Grid.Row="2" Header="DummyReadonlyCollection">
            <DataGrid ItemsSource="{Binding DummyReadonlyCollection}" />
        </GroupBox>

        <GroupBox Grid.Row="3" Header="Filtered view">
            <DataGrid ItemsSource="{Binding Filtered}" />
        </GroupBox>
    </Grid>
</UserControl>
