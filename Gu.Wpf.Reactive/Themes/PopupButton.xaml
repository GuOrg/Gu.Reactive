<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Gu.Wpf.Reactive">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Assets.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="DefaultPopupStyle" TargetType="{x:Type ToolTip}">
        <Setter Property="Placement" Value="Bottom" />
        <Setter Property="StaysOpen" Value="True" />
        <Setter Property="Background" Value="Tomato" />
        <Setter Property="PlacementTarget" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:PopupButton}}}" />
    </Style>

    <Style TargetType="{x:Type local:PopupButton}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="CornflowerBlue" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Template" Value="{StaticResource DefaultOverlayTemplate}" />
    </Style>
</ResourceDictionary>